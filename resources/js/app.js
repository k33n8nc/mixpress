import Alpine from "alpinejs";

window.Alpine = Alpine;

Alpine.start();

// gsap.from(".header-overlay", { duration: 0.7, y: 50, opacity: 0 });
// gsap.from("header h1", { duration: 1, opacity: 0 });
// gsap.from("header p", { duration: 1, opacity: 0 });
